sync:
  defaults:
    ignore:
      vcs: true
      paths:
        - ".DS_Store"
        - "node_modules"
        - ".git"

  membox: 
    alpha: "~/repo/membox"
    beta:  "edward@14.103.135.152:~/repo/membox"
    mode: "two-way-resolved"

# 启动命令 mutagen project start